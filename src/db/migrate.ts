/**
 * Applique les migrations manuellement : `npm run db:migrate`.
 * En temps normal ce n'est pas nécessaire — src/db/index.ts les applique
 * déjà au démarrage de l'application.
 */
import "./index";

console.log("Migrations appliquées.");
